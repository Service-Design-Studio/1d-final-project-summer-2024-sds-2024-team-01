<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ring of Reciprocity</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.7.2/font/bootstrap-icons.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css" rel="stylesheet">
  <%= stylesheet_link_tag "application.css"%>
</head>
  <body class="<%= "#{controller_name}-#{action_name}" %>">
    <%# <% unless controller_name == 'sessions' && action_name == 'new' || controller_name == 'registrations' && action_name == 'new' %> 
    <% unless current_page?(new_user_session_path) || current_page?(new_user_registration_path) || current_page?(new_user_password_path) %>
      <nav class="navbar fixed-top">
        <div class="container-fluid d-flex justify-content-start align-items-center">
          <a class="navbar-brand d-flex align-items-center" href="<%= root_path %>">
            <img src="/app/assets/images/OIG.jpg" alt="Logo" width="30" height="24" class="d-inline-block align-text-top">
            <span>Ring of Reciprocity</span>
          </a>

          <% if !user_signed_in? %>
            <%= link_to "Login", new_user_session_path, class: "btn ms-auto" %>
          <% else %>
            <div class="d-flex ms-4">
              <a class="nav-link" href="/myapplications">My Applications</a>
              <a class="nav-link" href="/myrequests">My Requests</a>
              <a class="nav-link" href="/about">About</a>
            </div>
            <div class="d-flex align-items-center ms-auto">
              <!-- New request button -->
              <a href="<%= new_request_path %>" class="btn custom-btn-dark-purple me-3">
                <i class="fas fa-plus"></i>
              </a>
              <!-- Profile on the right of Navbar -->
              <a href="<%= profile_path %>">
                <% if current_user.avatar.attached? %>
                  <img src="<%= image_url(current_user.avatar) %>" alt="Profile Picture" width="30" height="30" class="rounded-circle"> <!-- Profile pic -->
                <% end %>
                <span class="ms-2"><%= current_user.name %></span> <!-- Profile name -->
              </a>
              <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "btn  ms-5 " %>
            </div>
          <% end %>
        </div>
      </nav>
    <% end %>

    <div class="container mt-5 pt-5">
      <!-- Displaying notices and errors to user -->
      <% flash.each do |type, msg| %>
        <div>
          <%= msg %>
        </div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
