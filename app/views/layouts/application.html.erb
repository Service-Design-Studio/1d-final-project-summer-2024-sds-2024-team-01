<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ring of Reciprocity</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.7.2/font/bootstrap-icons.min.css" rel="stylesheet">
  <style>
    .navbar {
      background-color: #ad88c6 !important;
    }
    .content {
      padding-top: 80px; /* Adjust this value based on the height of your navbar */
    }
    .nav-link {
      margin-right: 40px; /* Adjust this value to increase spacing */
    }
    .custom-btn-dark-purple {
      background-color: #656dad;
      border-color: #656dad;
      color: white;
    }
    .custom-btn-dark-purple:hover {
      background-color: #9852a6; /* Slightly darker shade for hover effect */
      border-color: #9852a6;
    }
  </style>
</head>
    
    

  <body>
    <div class="container">
      <% if !user_signed_in? %>
        <%= link_to "Login", new_user_session_path, class: "btn btn-primary" %>
      <% else %>
        <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-primary" %>
      <% end %>
      <nav class="navbar fixed-top">
        <div class="container-fluid d-flex justify-content-start align-items-center">
          <a class="navbar-brand d-flex align-items-center" href="#">
            <img src="/app/assets/images/OIG.jpg" alt="Logo" width="30" height="24" class="d-inline-block align-text-top">
            <span>Ring of Reciprocity</span>
          </a>
          <div class="d-flex ms-4">
            <a class="nav-link" href="#">My Applications</a>
            <a class="nav-link" href="#">My Requests</a>
            <a class="nav-link" href="#">About</a>
          </div>
          <div class="d-flex align-items-center ms-auto">
            <!-- New request button -->
            <a href="<%= new_request_path %>" class="btn custom-btn-dark-purple me-3">
              <i class="fas fa-plus"></i>
            </a>
            <!-- Profile on the right of Navbar -->
            <img src="/path/to/profile.jpg" alt="Profile Picture" width="30" height="30" class="rounded-circle"> <!-- Profile pic -->
            <span class="ms-2">Vicky Jang</span> <!-- Profile name -->
          </div>
        </div>
      </nav>
    <%= yield %>
    </div>
  </body>
</html>
