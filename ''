class Cvm::EmployeesController < ApplicationController

  # Display all employees
  def index
    puts "heheeh"
    @allemployees = User.where(company_id: current_user.company_id)
  end

  # /POST cvm/employees/deactivate
  def deactivate
  end

  # /POST cvm/employees/activate
  def activate
  end
end
